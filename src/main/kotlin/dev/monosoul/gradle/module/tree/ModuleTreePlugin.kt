package dev.monosoul.gradle.module.tree

import org.gradle.api.Plugin
import org.gradle.api.initialization.Settings

class ModuleTreePlugin : Plugin<Settings> {
    override fun apply(target: Settings) = Unit
}